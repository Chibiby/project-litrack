/**
 * Override root `app/loading.tsx` dashboard skeletons on /login.
 * Login is a simple form — no content-slot skeleton is appropriate.
 */
export default function LoginLoading() {
  return null;
}
