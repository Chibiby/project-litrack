/**
 * Override `admin/loading.tsx` dashboard skeletons on /admin/login.
 * Login is a simple form — no content-slot skeleton is appropriate.
 */
export default function AdminLoginLoading() {
  return null;
}
