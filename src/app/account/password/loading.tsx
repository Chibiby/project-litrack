import { ContentRouteLoading } from "@/components/loading";

/** Minimal busy state for account password form. */
export default function AccountPasswordLoading() {
  return <ContentRouteLoading />;
}
