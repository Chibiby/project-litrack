import { ContentRouteLoading } from "@/components/loading";

/** Minimal busy state for account set-password form. */
export default function AccountSetPasswordLoading() {
  return <ContentRouteLoading />;
}
